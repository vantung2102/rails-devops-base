---
en:
  activerecord:
    errors:
      models:
        user:
          attributes:
            avatar:
              size: File size must be less than %{size} MB
  common:
    create:
      exists: "%{model} already existed"
      failed: Unable to create %{model}
      success: Created %{model} successfully
    delete:
      failed: Unable to delete %{model}
      success: Deleted %{model} successfully
    import:
      failed: Unable to import %{model}
      success: Import %{model} successfully
    update:
      failed: Unable to update %{model}
      success: Updated %{model} successfully
  validation:
    password_validation_message: Password must be at least 6 characters long and contain
      at least one uppercase letter, one lowercase letter, one number, and one special
      character
