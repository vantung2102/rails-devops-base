concurrency: 10
timeout: 25
pidfile: ./tmp/pids/sidekiq.pid
logfile: ./log/sidekiq.log

:queues:
  - default
  - low
  - medium
  - high

production:
  :concurrency: <%= ENV.fetch('SIDEKIQ_CONCURRENCY', 25) %>
